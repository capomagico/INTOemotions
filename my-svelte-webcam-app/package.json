{
  "name": "my-svelte-webcam-app",
  "version": "0.0.1",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "deploy": "vite build && vite gh-pages"
  },
  "dependencies": {
    "svelte": "^3.42.1"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^1.0.0",
    "vite": "^2.6.4",
    "vite-plugin-gh-pages": "^0.3.1"
  }
}
