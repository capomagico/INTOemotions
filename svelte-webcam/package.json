{
  "name": "svelte-webcam",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite", // Per avviare il server di sviluppo
    "build": "vite build", // Per creare la build del progetto
    "deploy": "gh-pages -d dist" // Per il deployment su GitHub Pages
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.1.1",
    "svelte": "^4.2.18",
    "vite": "^5.3.1",
    "gh-pages": "^4.0.0" // Aggiungi questa dipendenza per il deployment
  }
}
